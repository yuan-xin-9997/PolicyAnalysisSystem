## 1. 后端查询契约测试

- [x] 1.1 为筛选元数据响应、`full_text` 参数及标题关键词新语义补充 Pydantic 模型与路由契约失败测试
- [x] 1.2 为筛选选项去重/排序/仅返回有关联政策的数据及权限控制补充 API 和仓储失败测试
- [x] 1.3 为标题匹配、正文 FTS、中文短词 LIKE 回退、特殊字符字面处理及多条件 AND 组合补充服务/仓储失败测试

## 2. 后端筛选与检索实现

- [x] 2.1 扩展政策查询模型和列表路由白名单，增加经严格校验的 `full_text` 参数并将 `keyword` 限定为标题检索
- [x] 2.2 在仓储与服务层实现参数化标题包含匹配、正文 FTS/短词回退和组合查询，确保分页总数与结果使用相同条件
- [x] 2.3 实现受政策页面权限保护的 `GET /api/v1/policies/filters` 端点及稳定排序的发布部门、类别、来源查询
- [x] 2.4 运行政策后端测试并修复回归，确认详情 404、分页、时间范围和现有排序契约保持通过

## 3. 前端查询状态与筛选测试

- [x] 3.1 为 `full_text` 的 URL 序列化/恢复、清空筛选和默认排序补充查询工具失败测试
- [x] 3.2 为筛选元数据并行加载、原生下拉选项、已有 URL 值保留和独立错误状态补充政策列表组件失败测试
- [x] 3.3 为可见排序方向、`aria-sort`、切换行为及列表请求参数补充政策列表组件失败测试

## 4. 前端列表筛选与排序实现

- [x] 4.1 扩展前端 API 类型与政策查询状态，加入筛选元数据模型和独立正文全文检索字段
- [x] 4.2 将发布部门、类别 ID、来源 ID 输入框替换为数据库驱动的原生下拉框，并实现筛选元数据与列表的独立加载/错误处理
- [x] 4.3 增加正文全文检索框，明确标题关键词框文案，并保持筛选、分页、排序状态与 URL 双向一致
- [x] 4.4 为发布时间和最近抓取时间表头实现“最新优先/最早优先”的可见提示、方向图标、操作说明和 `aria-sort`

## 5. 政策详情阅读体验

- [x] 5.1 为长正文换行、类似 HTML 内容的纯文本安全渲染、元数据层级和窄屏布局补充政策详情组件失败测试
- [x] 5.2 重构政策详情语义结构，将标题、来源/类别、核心元数据、原文操作和正文阅读区分层展示
- [x] 5.3 增加受限阅读宽度、中文长文行高、`pre-wrap`、长文本断行和响应式样式，禁止使用 `v-html`

## 6. 集成验证与文档交付

- [x] 6.1 更新 mock API 与端到端用例，覆盖选择数据库筛选项、正文全文检索、排序提示和从列表进入详情的主路径
- [x] 6.2 运行完整后端单元/集成测试、前端单元测试、类型检查、构建、Playwright 冒烟测试及生命周期脚本测试并记录结果
- [x] 6.3 更新 README、需求规格说明书和设计说明书中的政策数据库页面、查询参数、筛选元数据接口及运维说明
- [x] 6.4 检查 Jenkinsfile 的现有构建与部署流程仍覆盖本次前后端测试且不覆盖 `src/config/app.json`，仅在验证发现缺口时调整
- [ ] 6.5 提交并推送变更后手动触发 Jenkins 构建，确认构建成功并提供部署后的政策数据库访问地址供验收
