## MODIFIED Requirements

### Requirement: 可读且安全的政策正文详情
系统 SHALL 在政策详情页分层展示标题、来源、类别、发布部门、发布时间、最近抓取时间和原文链接，并 SHALL 以段落分段、首行缩进、可自动折行且不会执行正文 HTML 的纯文本阅读布局展示完整正文。正文 SHALL 按换行与空行切分为段落，每个段落作为独立文本块渲染并施加首行缩进两个汉字宽度；段落来源于采集阶段从 HTML `<p>` 结构提取并以换行分隔的清洗后正文，因此分段 SHALL 不依赖 WebFetch 扁平化文本中的原始换行。对缺少换行的存量正文，系统 SHALL 提供安全兜底分段（整体作为单段渲染仍施加首行缩进），且 SHALL 不执行正文中的 HTML。

#### Scenario: 阅读长篇政策正文
- **WHEN** 用户打开包含多段、长行和无空格长文本的政策详情
- **THEN** 正文保留原有段落分隔、在容器内安全折行，并以适合长文阅读的宽度和行高显示而不产生横向撑破

#### Scenario: 正文按段落分段并首行缩进
- **WHEN** 政策正文为采集阶段从 HTML `<p>` 提取、以换行分隔的多段清洗后文本
- **THEN** 系统将正文按换行与空行切分为段落，每个段落作为独立文本块显示、段落间留有间距，且每个段落首行缩进两个汉字宽度

#### Scenario: 正文无原始换行仍按段落分段
- **WHEN** WebFetch `generic.article` 返回的扁平化正文不含任何换行，但采集阶段已从 HTML `<p>` 结构恢复段落并以换行存入 `content_text`
- **THEN** 详情页仍按 `content_text` 中的换行切分出多个段落并分别首行缩进，分段结果不因原始扁平文本无换行而退化为单段

#### Scenario: 存量无换行正文的安全兜底
- **WHEN** 历史政策的 `content_text` 既无换行也无段落结构（尚未重新抓取的存量数据）
- **THEN** 页面将该正文作为单个段落块安全渲染并施加首行缩进，不报错、不执行 HTML，且在该政策下次重新抓取后随段落化正文自然恢复分段

#### Scenario: 正文包含类似 HTML 的文本
- **WHEN** 政策正文包含标签或脚本形式的字符序列
- **THEN** 页面将其作为可见纯文本显示且不创建或执行对应 HTML

#### Scenario: 在窄屏查看政策详情
- **WHEN** 用户在窄屏设备打开政策详情
- **THEN** 标题、元数据、原文操作和正文自然换行且保持可访问，正文段落仍保持分段与首行缩进
